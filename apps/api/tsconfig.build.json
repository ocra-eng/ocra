{
  "extends": "./tsconfig.json",
  "include": ["src"],
  "compilerOptions": {
    "noEmit": false,
    // src (not ".") so output is dist/index.js, matching the start command.
    "rootDir": "src"
  }
}
